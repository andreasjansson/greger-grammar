=================
Tags in backticks
=================

# USER

Here's some code:

```
<eval>This should not be parsed</eval>
<!-- neither should this -->
<foo>nor this</foo>
```

What do you think? `test <no-parse>no!</no-parse>`

---

(source_file
  (user
    (user_header)
    (text)
    (code)
    (text)
    (code)
    (text)))
