=================
Tags in backticks
=================

# USER

Here's some code:

```
<eval>This should not be parsed</eval>
<!-- neither should this -->
<foo>nor this</foo>
```

What do you think? `test <no-parse>no!</no-parse>`

---

(source_file
  (user
    (user_header)
    (text)
    (code
      (code_start_tag)
      (code_content)
      (code_end_tag))
    (text)
    (code
      (code_start_tag)
      (code_content)
      (code_end_tag))
    (text)))
