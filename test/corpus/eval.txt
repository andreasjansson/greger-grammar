====
Eval
====

# SYSTEM

<eval>before</eval>

You are a helpful assistant.

<safe-shell-commands>
foo
<eval bash>echo bar</eval>
</safe-shell-commands>

<eval python>
after
<foo></foo>
</eval>

# USER

<eval>
before
</eval>

What's the weather like?

<eval>after</eval>

# ASSISTANT

Warm.

<eval>foo</eval>

---

(source_file
  (system
    (system_header)
    (eval
      (eval_start_tag)
      (eval_content)
      (eval_end_tag))
    (text)
    (safe_shell_commands
      (shell_command)
      (eval
        (eval_start_tag)
        (eval_content)
        (eval_end_tag)))
    (text)
    (eval
      (eval_start_tag)
      (eval_content)
      (eval_end_tag))
    (text))
  (user
    (user_header)
    (eval
      (eval_start_tag)
      (eval_content)
      (eval_end_tag))
    (text)
    (eval
      (eval_start_tag)
      (eval_content)
      (eval_end_tag))
    (text))
  (assistant
    (assistant_header)
    (text)))
