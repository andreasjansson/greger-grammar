================================================================================
Code close tag with mixed code blocks
================================================================================

# USER

```

<$code-close/>

`a` b

--------------------------------------------------------------------------------

(source_file
  (user
    (user_header)
    (code
      (code_start_tag)
      (code_content)
      (code_close_tag))
    (text)
    (code
      (code_start_tag)
      (code_content)
      (code_end_tag))
    (text)))

================================================================================
Code close tag immediately after backtick
================================================================================

# USER

`<$code-close/>

`a`

--------------------------------------------------------------------------------

(source_file
  (user
    (user_header)
    (code
      (code_start_tag)
      (code_close_tag))
    (text)
    (code
      (code_start_tag)
      (code_content)
      (code_end_tag))
    (text)))
